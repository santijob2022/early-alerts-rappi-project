
# Setting the environment

To use the same environment in local defined by project.toml as the kernel in jupyter notebooks, first register it with

python -m ipykernel install --user --name myenv --display-name "Python (myenv)"